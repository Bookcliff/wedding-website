import { Layout } from 'antd';
import React from 'react';

const { Content } = Layout;

export default function Registry() {
    return (
        <Layout>
            <Content>
                Registry
            </Content>
        </Layout>
    )
}